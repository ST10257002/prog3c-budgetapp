<?xml version="1.0" encoding="utf-8"?>
<manifest
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.BankBoosta"
            tools:targetApi="31"
            tools:replace="android:appComponentFactory"
            android:appComponentFactory="androidx.core.app.CoreComponentFactory">
        <activity
                android:name=".ui.views.LoginView"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.views.RegisterView"
            android:exported="false" />

        <activity
            android:name=".ui.views.CompleteProfileView"
            android:exported="false" />

        <activity
            android:name=".ui.views.GoalSettingView"
            android:exported="false" />

        <activity
            android:name=".ui.views.DashboardView"
            android:exported="false" />

        <activity
            android:name=".ui.views.TransactionsView"
            android:exported="false" />

        <activity
            android:name=".ui.views.AddIncomeView"
            android:exported="false" />

        <activity
            android:name=".ui.views.AddExpenseView"
            android:exported="false" />

    </application>

</manifest>